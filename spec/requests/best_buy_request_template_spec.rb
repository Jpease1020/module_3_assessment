require 'rails_helper'

describe "" do
  describe "GET /api/v1/???????????" do
    xit "returns ........." do
      #create some objects to have the endpoint return form the database
      # get "/api/v1/.......", {}, { "Accept" => "application/json" }
      # expect(response.status).to eq 200
      #
      # ?????? = JSON.parse(response.body)
      # asssert something
    end
  end
end
